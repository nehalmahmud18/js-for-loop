let num = 78;

while (num <= 98) { // Loop runs from 78 to 98
    console.log(num);

    if (num % 2 !== 0) { // Check for odd numbers
        console.log('Odd number:', num);
    }

    num++; // Increment num by 1
}